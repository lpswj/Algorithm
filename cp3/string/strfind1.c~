#include <stdio.h>
#include <string.h>
#define N 10000

main(int argc,char *argv[])
{
		int i,t,cnt=0;
		char a[N],*p = argv[1],*tmp;
		for(i = 0;i < N-1;a[i] = t,i++)
				if((t = getchar()) == EOF)
						break;
		a[i] = '\0';

}
