#include <math.h>

/* the header for avg.c and int.c */
typedef int Number;

/* the cartesian coordinate */
typedef struct {
		float x;
		float y;
}point;

/* the cartesian coordinate */
typedef struct {
		float r;
		float degree;
}point_1;

/* the triangle struct */
typedef struct {
		point A;
		point B;
		point C;
}triangle;


/* function provided to use */
Number randNum(int r);
Number randNum1(int r);
Number isLine(point p1,point p2,point p3);
float tri_area(triangle tr1);
/* generate a triple number */
int randNum2(float *a,triangle tr1);
