#include <stdio.h>
#include <math.h>

main()
{
		int N;
		double hn,r=0.57721;

		printf("Input the N: \n");
		scanf("%d",&N);

		hn = log(N) + r + (double)1/(12*N);

		printf("Hn is %.4f\n",hn);


}
